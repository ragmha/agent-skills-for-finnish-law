name: Validoi plugarit

# Ajaa rakenne- ja laatuvalidaattorin jokaisessa pushissa ja pull requestissa.
# Portti estää markkinapaikan rikkovat virheet (frontmatter, marketplace-
# yhtenäisyys, kuolleet linkit, Cowork-validaattorin numero-pilkku-ansa)
# ennen kuin ne päätyvät käyttäjien asennuksiin.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Validoi rakenne ja frontmatter
        run: node scripts/validate.mjs

      - name: Tarkista repon invariantit
        run: node scripts/check-invariants.mjs

      - name: Tarkista tuotoskielen ilmoitukset
        run: node scripts/check-output-language.mjs

      - name: Tarkista turvamekanismien eheys
        run: node scripts/check-safety-mechanisms.mjs

      - name: Tarkista viittausten eheys
        run: node scripts/check-citations.mjs

      - name: Tarkista kuvaukset
        run: node scripts/check-descriptions.mjs

      - name: Tarkista että generoidut tiedostot ovat ajan tasalla
        run: bash scripts/check-generated.sh

      - name: Aja Node-testit
        run: node --test tests/*.test.mjs

  portability-tests:
    name: Node-testit (windows-latest)
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Aja Node-testit
        run: node --test tests/*.test.mjs
